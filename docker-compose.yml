services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "8095:8095"
    command: uvicorn observability.api:app --host 0.0.0.0 --port 8095

  dashboard:
    build: .
    env_file:
      - .env
    ports:
      - "8501:8501"
    command: streamlit run dashboard/app.py --server.address 0.0.0.0 --server.port 8501
